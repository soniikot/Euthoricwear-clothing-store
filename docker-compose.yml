services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "5173:5173"

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "1337:1337"
